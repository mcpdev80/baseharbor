name: runtime-image

on:
  workflow_call:
    inputs:
      ref:
        description: Source ref to build and publish
        required: false
        type: string
      version_tag:
        description: Release tag used for image metadata, for example v0.4.15 or v0.4.15.1
        required: false
        type: string
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve source ref
        id: source
        shell: bash
        env:
          INPUT_REF: ${{ inputs.ref }}
          INPUT_VERSION_TAG: ${{ inputs.version_tag }}
        run: |
          set -euo pipefail

          if [ -n "${INPUT_REF:-}" ]; then
            ref="${INPUT_REF}"
          elif [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            ref="${GITHUB_REF_NAME}"
          else
            ref="${GITHUB_SHA}"
          fi

          if [ -n "${INPUT_VERSION_TAG:-}" ]; then
            version_tag="${INPUT_VERSION_TAG}"
          elif [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            version_tag="${GITHUB_REF_NAME}"
          else
            version_tag=""
          fi

          if [ -n "${version_tag}" ] && [[ ! "${version_tag}" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(\.(0|[1-9][0-9]*))?(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "Runtime release tag must use vMAJOR.MINOR.PATCH or vMAJOR.MINOR.PATCH.HOTFIX, optionally followed by a prerelease suffix." >&2
            exit 1
          fi

          echo "ref=${ref}" >> "${GITHUB_OUTPUT}"
          echo "version_tag=${version_tag}" >> "${GITHUB_OUTPUT}"

      - name: Checkout
        uses: actions/checkout@v7.0.1
        with:
          ref: ${{ steps.source.outputs.ref }}

      - name: Resolve build metadata
        id: buildmeta
        shell: bash
        env:
          VERSION_TAG: ${{ steps.source.outputs.version_tag }}
        run: |
          set -euo pipefail
          if [ -n "${VERSION_TAG:-}" ]; then
            version="${VERSION_TAG#v}"
            release_core="${version%%-*}"
            IFS='.' read -r major minor patch hotfix_extra <<<"${release_core}"
            minor_tag="${major}.${minor}"
            if [[ "${version}" == *-* ]]; then
              stable="false"
            else
              stable="true"
            fi
          else
            version="edge"
            minor_tag=""
            stable="false"
          fi
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          echo "minor_tag=${minor_tag}" >> "${GITHUB_OUTPUT}"
          echo "stable=${stable}" >> "${GITHUB_OUTPUT}"
          echo "date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "${GITHUB_OUTPUT}"
          echo "commit=$(git rev-parse HEAD)" >> "${GITHUB_OUTPUT}"
          echo "short_commit=$(git rev-parse --short=7 HEAD)" >> "${GITHUB_OUTPUT}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/mcpdev80/baseharbor-runtime
          flavor: |
            latest=false
          tags: |
            type=raw,value=${{ steps.buildmeta.outputs.version }},enable=${{ steps.source.outputs.version_tag != '' }}
            type=raw,value=${{ steps.buildmeta.outputs.minor_tag }},enable=${{ steps.buildmeta.outputs.stable == 'true' }}
            type=raw,value=latest,enable=${{ steps.buildmeta.outputs.stable == 'true' }}
            type=raw,value=edge,enable=${{ steps.source.outputs.version_tag == '' }}
            type=raw,value=sha-${{ steps.buildmeta.outputs.short_commit }}

      - name: Build runtime image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/control-plane/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ steps.buildmeta.outputs.version }}
            COMMIT=${{ steps.buildmeta.outputs.commit }}
            DATE=${{ steps.buildmeta.outputs.date }}

      - name: Attest runtime image
        uses: actions/attest@v4
        with:
          subject-name: ghcr.io/mcpdev80/baseharbor-runtime
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
