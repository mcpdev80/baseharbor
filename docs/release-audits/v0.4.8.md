# v0.4.8 release audit

This audit tracks the mandatory engineering and documentation gates for the Prometheus metrics platform integration.

## Architecture boundary

- [x] Application intent describes an application-provided `metrics/v1` source, never Prometheus.
- [x] OpenMetrics-compatible HTTP exposition is signal semantics; provider choice is separate.
- [x] Collection enablement is deployment/operator policy.
- [x] Prometheus is a replaceable Provider Integration Contract v1 reference implementation.
- [x] OTLP metrics export remains a separate `telemetry.otlp/v1` concern.

## Lifecycle and ownership

- [x] Preflight validates source semantics and policy before provider mutation.
- [x] Prometheus placement is resolved through the generic provider-placement model rather than metrics-specific scope policy.
- [x] Safe default shared Prometheus provisioning is idempotent; application-scoped placement is independently isolated.
- [x] Named shared boundaries create independent provider projects/state and do not widen access to unrelated applications.
- [x] Targets are generated automatically through protected BaseHarbor state.
- [x] Only declared/authorized metrics-source workload services join their application's isolated metrics network.
- [x] Application destroy removes only that application's target/trust-edge state or its dedicated provider, according to placement.
- [x] Shared provider lifetime is independent of one application's lifetime.
- [x] Global `baha down` non-destructively stops shared Platform/Core Runtime providers while preserving shared networks/volumes/state; `baha up` resumes them.
- [x] Registered/current placement is persisted and used for down/destroy cleanup instead of trusting transient override environment.
- [x] Placement migration converges/verifies the new path before removing the previous registered metrics placement.
- [x] Runtime-only `metrics/v1` permissions receive a protected provider-registry binding even without static `metrics.sources`.
- [x] Global destroy enumerates and removes all BaseHarbor-owned shared Prometheus default/sharing-boundary instances and data volumes.

## Cross-application connectivity

- [x] Connectivity is a separate platform policy and is not inferred from provider sharing.
- [x] One directional rule is the source of truth; applications do not duplicate declarations.
- [x] Compose does not attach source and target to one common bridge network.
- [x] A connection-specific source link plus hardened BaseHarbor Runtime relay preserves directionality.
- [x] Relay exposes no host port or runtime socket, runs non-root/read-only, drops capabilities and uses no-new-privileges.
- [x] Target port is derived from known provider/runtime state when unambiguous and requires explicit qualification otherwise.
- [x] app down suspends runtime connectivity while preserving policy; app up/apply reconcile it.
- [x] app/global destroy fails closed while connectivity policy still references managed applications.
- [ ] Live Compose test proves source -> target succeeds while target -> source has no reciprocal path.

## Isolation and security

- [x] Deterministic app/environment/service aliases prevent same-name workload service collisions.
- [x] Target files are application/environment scoped.
- [x] Unit coverage proves pruning one application does not remove another application's targets.
- [x] Prometheus receives no Docker/Podman socket.
- [x] Prometheus uses a read-only root filesystem, dropped capabilities and no-new-privileges.
- [x] Prometheus API is host-loopback-only.
- [x] Target state contains no credentials or secret values.
- [x] Metrics collection does not provision Grafana, Loki or Tempo.

## Verification

- [x] Unit tests cover manifest round-trip and fail-closed validation.
- [x] Provider contract conformance includes `metrics/v1`.
- [x] Runtime metadata does not claim a Prometheus binding when collection is disabled.
- [x] Workload networking tests cover source-service-only attachment.
- [x] Provider tests cover generated configuration, attribution and cross-application target pruning.
- [x] Placement tests cover safe defaults, explicit supported override, unsupported fail-closed behavior, named shared boundaries and registered-vs-desired placement separation.
- [ ] Manual Docker acceptance proves two isolated applications are scraped by one shared Prometheus provider.
- [ ] Exact final implementation head passes gofmt, tests, vet and build.
- [ ] EN/DE MkDocs strict builds pass on the exact final documentation head.
- [ ] Workflow YAML passes actionlint on the exact final head.

## Documentation

- [x] Capability Specification added.
- [x] Application contract updated in EN/DE.
- [x] Architecture updated in EN/DE.
- [x] Capability/provider model updated in EN/DE.
- [x] CLI collection policy updated in EN/DE.
- [x] Provider Integration Contract conformance guidance updated in EN/DE.
- [x] Roadmap updated in EN/DE.
- [x] Changelog updated.
- [x] Human-friendly release notes added at `docs/releases/v0.4.8.md`.
- [x] Development Guidelines reviewed against the current final architecture, isolation, ownership, secret-safety, fail-closed behavior and lifecycle responsibilities; review findings were fixed before validation.
- [ ] Final stale-version/status search reconciled for the exact final implementation/docs head.

## Live validation

Run on the target Compose host before release:

```bash
bash scripts/validate-v048-live.sh
```

The script records the exact Git head, checks whitespace/gofmt, runs the full Go test suite, vet and CLI build, builds the BaseHarbor Runtime image from that exact checkout, and then runs the real Prometheus, managed-exposure and directed cross-application connectivity acceptances. If MkDocs is installed locally, it also runs both strict documentation builds.

This local run is the preferred first release gate because it validates the exact checkout without consuming GitHub Actions. The manual `ci` workflow remains the final hosted evidence gate immediately before merge/release.

- [ ] Local live validation passed on the intended Compose host and the exact tested head is recorded.
- [ ] Manual `ci` workflow passed on that same exact head after local validation.

## Release completion

The release issue is not complete until:

- [ ] final acceptance is green;
- [ ] the feature PR is merged;
- [ ] changelog/release metadata is cut consistently for v0.4.8;
- [ ] the release workflow completes successfully;
- [ ] published GitHub release artifacts are verified.
