# v0.4.9 release audit

Issue: #164

## Scope

v0.4.9 delivers provider-neutral centralized workload logs through Loki/Alloy and closes the hardening findings added after the v0.4.8 architecture/security review.

## Implementation audit

- [x] `logs/v1` is versioned and provider-neutral.
- [x] Loki is registered through Provider Integration Contract v1.
- [x] Loki supports shared and application placement; unsupported external placement fails closed.
- [x] Shared named boundaries remain a property of shared placement, not a new scope.
- [x] logical log resources are application-owned and stored in the protected provider registry.
- [x] real readiness checks Loki `/ready`; verification queries Loki for application/environment/service streams.
- [x] no implicit Grafana, Tempo or application-visible S3 capability.
- [x] Compose workload security preflight analyzes the rendered Compose model before start/mutation.
- [x] allow/warn/deny policy and machine-readable diagnostics are covered by unit tests.
- [x] provider conformance is executable and reusable.
- [x] fake provider covers CREATE, NOOP, DRIFT/REPAIR and fail-closed fault paths.
- [x] retry/fault-injection tests cover malformed binding, verify failure and ownership-safe destroy.
- [x] provider/core switch audit completed.
- [x] no Kubernetes/OpenShift runtime implementation added; arbitrary-UID compatibility is defined as a runtime/provider security requirement.
- [x] current BaseHarbor-managed Compose runtimes are rendered with explicit non-root identities or BaseHarbor-owned non-root images.
- [x] managed runtime hardening covers PostgreSQL, Valkey, OpenBao, Caddy, SeaweedFS, OTLP Collector, Prometheus, Loki, Alloy, Broker, Executor and Connectivity Relay.
- [x] BaseHarbor-owned runtime images prepare writable state paths for future platform-assigned arbitrary non-root UIDs.

## Security review

The v0.4.9 security pass applies the managed-container baseline across the current Compose reference runtime: UID 0 is not a valid normal runtime identity; capabilities are dropped, privilege escalation is disabled and root filesystems are read-only where the service permits it, with only explicit writable volumes/tmpfs retained. Caddy uses high unprivileged container ports rather than relying on bind-service capability. Loki and Alloy receive no Docker/Podman socket, and host-facing API/syslog listeners bind only to loopback.

Compose service identities remain runtime realization rather than portable intent. BaseHarbor-owned runtime images additionally prepare writable state directories for arbitrary platform-assigned non-root UIDs, preserving the future OpenShift/Kubernetes boundary without claiming those runtimes are implemented in v0.4.9.

Repository workloads are checked for privileged mode, container-runtime sockets, host network/PID/IPC namespaces, dangerous capabilities, devices and critical host bind mounts. Managed environments deny isolation bypasses before mutation. Development-only exceptions require explicit acknowledgement.

Provider state and registrations are owner-only. No secret value is written to Loki registration state, diagnostics or labels.

## Provider switch/core-coupling audit

A duplicated application-layer provider descriptor switch was removed; the application registry now consumes `capability.ReferenceIntegration` as the canonical descriptor mapping.

The remaining switches are intentional:

- capability -> reference provider resolution: authoritative reference-provider policy;
- provider -> default placement: authoritative deployment policy;
- provider placement -> runtime topology inside a concrete provider: provider implementation detail;
- named-resource provider instance IDs for SQL/KV: stable identity compatibility behavior.

No speculative global provider state machine was introduced.

## Compatibility

Manifest v1 remains unchanged. Logs are selected by platform/deployment policy and repository workload discovery rather than a Loki field in portable application intent.

## Validation policy

Validation is local/Hugging Face first. GitHub Actions remain manual-only except the version-tag release workflow. No broad GitHub Actions matrix is required for development commits.

Evidence:

- executable/product tree at `98793afc528e1e2d40f06ffda7f2cd6055fca602`: gofmt, full `go test ./...`, `go vet ./...`, CLI build and `git diff --check` PASS — Hugging Face job `6aaf40a551992417dfccbaf9`; including explicit multi-service application-scoped Loki registry ownership coverage;
- strict EN/DE MkDocs on release-preparation tree: PASS — Hugging Face job `6aaf3fa351992417dfccbab1`;
- real Loki 3.7.8 + Alloy 1.19.2 binary data flow: RFC5424 UDP -> Alloy relabel -> Loki ingestion -> LogQL query PASS — Hugging Face job `6aaf3f6f52d0dbd7f1d71e2b`;
- all GitHub workflow YAML, including the v0.4.9 tag-release Loki Compose acceptance gate: `actionlint` PASS at `49ed7c16931db8dc48df4f29a5620944fee83d36` — Hugging Face job `6aaf403252d0dbd7f1d71e6a`.

Hugging Face Jobs does not expose a Docker daemon, so the repository also carries `TestManagedLokiIngestsRealComposeWorkloadLogs` as a real Docker/Podman Compose acceptance. The existing release workflow executes that single targeted acceptance automatically on the v0.4.9 tag before GoReleaser. No development GitHub Actions workflow was triggered for this implementation.

## Release closeout

- [x] exact implementation/release-preparation validation recorded;
- [x] final EN/DE documentation build green;
- [x] human-friendly release notes verified;
- [x] Development Guidelines checked against final diff;
- [ ] PR merged to main;
- [ ] v0.4.9 tag created on exact green main commit;
- [ ] release workflow green;
- [ ] GitHub Release/artifacts/checksums/provenance/runtime image verified.
