# v0.4.9 release audit

Issue: #164

## Scope

v0.4.9 delivers provider-neutral centralized workload logs through Loki/Alloy and closes the hardening findings added after the v0.4.8 architecture/security review.

## Implementation audit

- [x] `logs/v1` is versioned and provider-neutral.
- [x] Loki is registered through Provider Integration Contract v1.
- [x] Loki supports shared and application placement; unsupported external placement fails closed.
- [x] Shared named boundaries remain a property of shared placement, not a new scope.
- [x] logical log resources are application-owned and stored in the protected provider registry.
- [x] real readiness checks Loki `/ready`; verification queries Loki for application/environment/service streams.
- [x] no implicit Grafana, Tempo or application-visible S3 capability.
- [x] Compose workload security preflight analyzes the rendered Compose model before start/mutation.
- [x] allow/warn/deny policy and machine-readable diagnostics are covered by unit tests.
- [x] provider conformance is executable and reusable.
- [x] fake provider covers CREATE, NOOP, DRIFT/REPAIR and fail-closed fault paths.
- [x] retry/fault-injection tests cover malformed binding, verify failure and ownership-safe destroy.
- [x] provider/core switch audit completed.
- [x] no Kubernetes/OpenShift implementation added.

## Security review

Loki and Alloy are BaseHarbor-owned provider components with read-only roots, dropped capabilities, no-new-privileges and no Docker/Podman socket. Host-facing API/syslog listeners bind only to loopback.

Repository workloads are checked for privileged mode, container-runtime sockets, host network/PID/IPC namespaces, dangerous capabilities, devices and critical host bind mounts. Managed environments deny isolation bypasses before mutation. Development-only exceptions require explicit acknowledgement.

Provider state and registrations are owner-only. No secret value is written to Loki registration state, diagnostics or labels.

## Provider switch/core-coupling audit

A duplicated application-layer provider descriptor switch was removed; the application registry now consumes `capability.ReferenceIntegration` as the canonical descriptor mapping.

The remaining switches are intentional:

- capability -> reference provider resolution: authoritative reference-provider policy;
- provider -> default placement: authoritative deployment policy;
- provider placement -> runtime topology inside a concrete provider: provider implementation detail;
- named-resource provider instance IDs for SQL/KV: stable identity compatibility behavior.

No speculative global provider state machine was introduced.

## Compatibility

Manifest v1 remains unchanged. Logs are selected by platform/deployment policy and repository workload discovery rather than a Loki field in portable application intent.

## Validation policy

Validation is local/Hugging Face first. GitHub Actions remain manual-only except the version-tag release workflow. No broad GitHub Actions matrix is required for development commits.

Evidence recorded so far:

- Hugging Face `go test ./...`: PASS on branch state after workload-security/conformance/Loki implementation (`6aaf3c8f52d0dbd7f1d71d31`).

The final release-preparation head must additionally pass gofmt, full Go tests, vet, build, strict EN/DE MkDocs and targeted Loki/Alloy configuration/data-flow acceptance. Exact final-head evidence is recorded before merge/tag.

## Release closeout

- [ ] exact final head validation recorded;
- [ ] final EN/DE documentation build green;
- [ ] human-friendly release notes verified;
- [ ] Development Guidelines checked against final diff;
- [ ] PR merged to main;
- [ ] v0.4.9 tag created on exact green main commit;
- [ ] release workflow green;
- [ ] GitHub Release/artifacts/checksums/provenance/runtime image verified.
